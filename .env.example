# Server-only key used by the Eve runtime. Never expose it with NEXT_PUBLIC_.
OPENROUTER_API_KEY=

# OpenRouter chooses a suitable tool-capable model for each prompt.
OPENROUTER_MODEL=openrouter/auto
